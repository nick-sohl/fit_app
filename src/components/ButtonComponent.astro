---
const {primary, secondary, buttonText, buttonType} = Astro.props;
---
<button 
  type={buttonType} 
  class={
  `rounded-md px-3 py-2 text-sm font-semibold text-white hover:bg-indigo-400 focus-visible:outline-2 focus-visible:outline-offset-2 
  ${primary ? 'bg-indigo-500 focus-visible:outline-indigo-500 hover:bg-indigo-400' 
  : 'bg-white/10 hover:bg-white/20 inset-ring inset-ring-white/5'}`}>
  {buttonText}
</button>
